:root {
  --black: #000;
  --dark: #0a0a0a;
  --purple-dark: #1a0b2e;
  --purple: #2d1b4e;
  --accent: #00ff00;
  --accent-dim: rgba(0, 255, 0, 0.15);
  --orange: #ff6b35;
  --white: #fff;
  --text: rgba(255, 255, 255, 0.88);
  --text-muted: rgba(255, 255, 255, 0.6);
  --border: rgba(255, 255, 255, 0.12);
  --font: 'Inter', system-ui, sans-serif;
  --radius: 12px;
  --radius-lg: 16px;
  --transition: 0.3s ease;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; scroll-padding-top: 80px; }
body {
  font-family: var(--font);
  background: var(--black);
  color: var(--text);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
img { max-width: 100%; display: block; }
a { color: inherit; text-decoration: none; }

.container { width: 100%; max-width: 1140px; margin: 0 auto; padding: 0 1.5rem; }

/* Header */
.header {
  position: fixed; top: 0; left: 0; width: 100%; z-index: 1000;
  padding: 1.25rem 0; background: transparent;
  transition: background 0.3s ease, padding 0.3s ease;
}
.header.scrolled {
  background: rgba(0,0,0,0.95);
  backdrop-filter: blur(12px);
  padding: 0.9rem 0;
}
.header-inner { display: flex; align-items: center; justify-content: space-between; }
.logo img { height: 48px; width: auto; }
.nav-left, .nav-right { display: flex; gap: 1.75rem; }
.nav-link {
  font-size: 0.95rem; font-weight: 600; text-transform: uppercase;
  letter-spacing: 0.04em; opacity: 0.85; transition: 0.2s ease;
}
.nav-link:hover { opacity: 1; color: var(--accent); }

/* Buttons */
.btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
  padding: 1rem 2.2rem; border-radius: 999px; font-weight: 800; font-size: 1rem;
  text-transform: uppercase; letter-spacing: 0.05em; border: 2px solid transparent;
  cursor: pointer; transition: all 0.3s ease;
}
.btn-primary {
  background: var(--black); color: var(--white); position: relative; z-index: 1; overflow: hidden;
}
.btn-primary::before {
  content: ''; position: absolute; inset: 0;
  background: linear-gradient(135deg, #5B2C8E, #8B4BD4, #66ff99, #00ff00);
  z-index: -1; opacity: 0; transition: opacity 0.35s ease;
}
.btn-primary:hover::before { opacity: 1; }
.btn-primary:hover { transform: translateY(-3px); box-shadow: 0 12px 28px rgba(91,44,142,0.45); }
.btn-outline {
  background: transparent; border-color: rgba(255,255,255,0.3); color: var(--white);
}
.btn-outline:hover { border-color: var(--accent); color: var(--accent); transform: translateY(-3px); }
.btn-sm { padding: 0.75rem 1.6rem; font-size: 0.9rem; }
.btn-block { width: 100%; }

/* Hero */
.hero {
  position: relative; min-height: 100vh; display: flex; align-items: center;
  justify-content: center; text-align: center; padding: 8rem 0 5rem; overflow: hidden;
}
.hero-bg {
  position: absolute; inset: 0;
  background: url('../images/hero-bg.jpg') center/cover no-repeat;
  opacity: 0.45; z-index: -2;
}
.hero-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(45deg, rgba(42,15,84,0.85), rgba(26,11,46,0.92));
  z-index: -1;
}
.hero-content { position: relative; z-index: 1; max-width: 800px; }
.hero-logo {
  height: 160px; width: auto; margin: 0 auto 2.5rem; border-radius: 50%;
  border: 3px solid rgba(0,255,0,0.25); padding: 1rem; background: rgba(0,0,0,0.3);
  filter: drop-shadow(0 0 25px rgba(0,255,0,0.3)); transition: transform 0.6s ease;
}
.hero-logo:hover { transform: rotateY(180deg) scale(1.05); border-color: var(--accent); }
.hero-title {
  font-size: clamp(2.8rem, 8vw, 4.8rem); font-weight: 900; text-transform: uppercase;
  line-height: 1; color: var(--white); margin-bottom: 1rem;
  text-shadow: 0 0 40px rgba(0,255,0,0.25);
}
.hero-subtitle {
  font-size: clamp(1.25rem, 3vw, 1.7rem); color: rgba(255,255,255,0.9);
  margin-bottom: 2.75rem; line-height: 1.4;
}
.hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; margin-bottom: 3rem; }
.hero-proof { display: flex; flex-direction: column; align-items: center; gap: 1rem; }
.live-badge { display: flex; align-items: center; gap: 0.6rem; font-size: 0.95rem; font-weight: 500; }
.pulse {
  width: 10px; height: 10px; background: var(--accent); border-radius: 50%;
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(1.3); }
}
.trust-row { display: flex; gap: 0.75rem; font-size: 0.9rem; color: var(--text-muted); flex-wrap: wrap; justify-content: center; }

/* Sections */
.section { padding: 6rem 0; }
.section-header { text-align: center; margin-bottom: 3.5rem; }
.section-title {
  font-size: clamp(2rem, 5vw, 3rem); font-weight: 900; text-transform: uppercase;
  color: var(--white); margin-bottom: 0.75rem;
  text-shadow: 0 0 30px rgba(0,255,0,0.2);
}
.section-subtitle { font-size: 1.25rem; color: rgba(255,255,255,0.85); max-width: 560px; margin: 0 auto; }

/* About */
.about-grid {
  display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; margin-bottom: 4rem;
}
.about-grid h3 { font-size: 1.6rem; color: var(--white); margin-bottom: 1.25rem; }
.about-grid p { margin-bottom: 1.25rem; }
.callout {
  background: var(--accent-dim); border-left: 4px solid var(--accent);
  padding: 1.25rem 1.5rem; border-radius: var(--radius); font-weight: 600; color: var(--accent);
}
.features { display: grid; gap: 1.5rem; }
.feature { display: flex; gap: 1rem; align-items: flex-start; }
.feature span { font-size: 1.75rem; }
.feature strong { display: block; color: var(--white); margin-bottom: 0.25rem; }

/* Audio Demo */
.audio-demo {
  background: rgba(0,0,0,0.45); border: 1px solid var(--border);
  border-radius: var(--radius-lg); padding: 2.5rem; text-align: center;
}
.audio-demo h3 { color: var(--accent); font-size: 1.5rem; margin-bottom: 0.5rem; }
.audio-demo > p { color: var(--text-muted); margin-bottom: 2rem; }
.audio-demo .raw { color: var(--orange); font-weight: 600; }
.audio-demo .mixed { color: var(--accent); font-weight: 600; }
.crossfade { max-width: 420px; margin: 0 auto 1.5rem; }
.crossfade-labels {
  display: flex; justify-content: space-between; font-size: 0.9rem; font-weight: 700; margin-bottom: 0.6rem;
}
#crossfadeSlider {
  width: 100%; height: 8px; -webkit-appearance: none; appearance: none;
  background: linear-gradient(to right, var(--orange), var(--accent));
  border-radius: 4px; outline: none; cursor: pointer;
}
#crossfadeSlider::-webkit-slider-thumb {
  -webkit-appearance: none; width: 22px; height: 22px; border-radius: 50%;
  background: #fff; border: 3px solid var(--accent); cursor: pointer;
}
.audio-btns { display: flex; gap: 1rem; justify-content: center; margin-bottom: 1rem; }
.audio-status { font-size: 0.9rem; color: var(--text-muted); }

/* Pricing */
.pricing-grid {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.75rem; margin-bottom: 3rem;
}
.price-card {
  background: rgba(255,255,255,0.04); border: 1px solid var(--border);
  border-radius: var(--radius-lg); padding: 2rem 1.75rem; text-align: center;
  position: relative; transition: 0.3s ease;
}
.price-card:hover { transform: translateY(-6px); border-color: rgba(0,255,0,0.35); }
.price-card.popular { border: 2px solid var(--accent); }
.popular-badge {
  position: absolute; top: -12px; left: 50%; transform: translateX(-50%);
  background: var(--accent); color: var(--black); font-weight: 900; font-size: 0.8rem;
  padding: 0.3rem 1rem; border-radius: 999px; text-transform: uppercase;
}
.price-icon { font-size: 2.2rem; margin-bottom: 0.75rem; }
.price-card h3 { font-size: 1.5rem; color: var(--white); margin-bottom: 0.5rem; }
.price { font-size: 2.8rem; font-weight: 900; color: var(--accent); line-height: 1; margin-bottom: 0.35rem; }
.price-desc { color: var(--text-muted); margin-bottom: 1.5rem; }
.price-card ul { list-style: none; margin-bottom: 1.75rem; text-align: left; }
.price-card li {
  padding: 0.55rem 0; border-bottom: 1px solid rgba(255,255,255,0.08);
  display: flex; align-items: center; gap: 0.6rem;
}
.price-card li::before { content: "✓"; color: var(--accent); font-weight: 700; }

.rates {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 1rem; margin-bottom: 2rem; text-align: center;
}
.rate {
  background: rgba(255,255,255,0.04); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 1.25rem 1rem;
}
.rate span { display: block; font-size: 0.85rem; color: var(--text-muted); margin-bottom: 0.35rem; }
.rate strong { font-size: 1.4rem; color: var(--accent); }

.deposit-note {
  text-align: center; background: rgba(255,107,53,0.15); border: 1px solid var(--orange);
  border-radius: var(--radius); padding: 1.25rem; font-weight: 700;
}

/* Testimonials */
.testimonials { background: rgba(26,11,46,0.4); }
.testimonials-grid {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem;
}
.testimonial {
  background: rgba(0,0,0,0.35); border: 1px solid var(--border);
  border-radius: var(--radius-lg); padding: 1.75rem;
}
.testimonial p { font-size: 1.1rem; margin-bottom: 1.25rem; font-style: italic; }
.author { color: var(--accent); font-weight: 600; font-size: 0.95rem; }

/* Portfolio */
.portfolio-tabs { display: flex; gap: 1rem; justify-content: center; margin-bottom: 2rem; flex-wrap: wrap; }
.portfolio-tab {
  padding: 0.9rem 1.75rem; background: rgba(255,255,255,0.05); border: 1px solid var(--border);
  border-radius: 999px; color: var(--white); font-weight: 600; cursor: pointer; transition: 0.3s ease;
}
.portfolio-tab.active, .portfolio-tab:hover {
  background: transparent; border-color: var(--accent); color: var(--accent);
}
.portfolio-content { display: none; text-align: center; max-width: 600px; margin: 0 auto; }
.portfolio-content.active { display: block; }

/* Contact */
.contact-form {
  max-width: 640px; margin: 0 auto; background: rgba(255,255,255,0.04);
  border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 2.5rem;
}
.form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }
.form-group { margin-bottom: 1.25rem; }
label { display: block; font-weight: 600; margin-bottom: 0.4rem; color: rgba(255,255,255,0.85); }
input, select, textarea {
  width: 100%; padding: 0.9rem 1rem; background: rgba(255,255,255,0.07);
  border: 1px solid var(--border); border-radius: var(--radius); color: var(--white);
  font: inherit; transition: 0.2s ease;
}
input:focus, select:focus, textarea:focus { outline: none; border-color: var(--accent); }
textarea { resize: vertical; min-height: 120px; }

/* Footer */
.footer {
  background: #050505; border-top: 1px solid var(--border);
  padding: 2.5rem 0; text-align: center; color: var(--text-muted); font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 768px) {
  .nav-left, .nav-right { display: none; }
  .about-grid { grid-template-columns: 1fr; }
  .form-row { grid-template-columns: 1fr; }
  .hero-logo { height: 120px; }
  .hero-buttons { flex-direction: column; align-items: center; }
  .btn { width: 100%; max-width: 280px; }
}
